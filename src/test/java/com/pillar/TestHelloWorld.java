package com.pillar;

import org.junit.Assert;
import org.junit.Test;

public class TestHelloWorld {

    private PillarWorld pillarWorld = new PillarWorld();


    @Test
    public void pillarWorldTest() {
        String pillarTest = pillarWorld.pillar("pillar");
        Assert.assertEquals("pillarTEST", pillarTest);
    }

    @Test
    public void pillarWorld1Test() {
        String pillarTest = pillarWorld.pillar("pillar");
        Assert.assertEquals("pillarTEST", pillarTest);
    }

    @Test
    public void pillarWorld2Test() {
        String pillarTest = pillarWorld.pillar("pillar");
        Assert.assertEquals("pillarTEST", pillarTest);
    }

    @Test
    public void pillarWorld3Test() {
        String pillarTest = pillarWorld.pillar("pillar");
        Assert.assertEquals("pillarTEST", pillarTest);
    }

    @Test
    public void pillarWorld4Test() {
        String pillarTest = pillarWorld.pillar("pillar");
        Assert.assertEquals("pillarTEST", pillarTest);
    }

    @Test
    public void pillarWorld5Test() {
        String pillarTest = pillarWorld.pillar("pillar");
        Assert.assertEquals("pillarTEST", pillarTest);
    }

}
